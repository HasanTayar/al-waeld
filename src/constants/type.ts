export type TranslationForPageProps = {
  pageName: string;
  langCode: string;
};

export type langModal = {
  title?: string;
  description?: string;
  submit?: string;
  isFirstVisit?: boolean;
};
